projection;
strict ( 2 );
use draft;

define behavior for ZC_EMPLOY_DM //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Discard;
  use action Resume;
  use action Activate;
  use action Prepare;
}